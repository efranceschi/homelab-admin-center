# Test/CI dependencies for the web panel and the Ansible-side Python.
# Installed on top of requirements-web.txt (panel runtime) in CI and locally.
-r requirements-web.txt

pytest>=9.0.3
pytest-cov>=7.1.0
pytest-asyncio>=1.4.0
httpx>=0.28.1            # required by fastapi.testclient.TestClient

# Needed to import the Ansible connection plugin (plugins/connection/pct.py)
# in tests/test_connection_pct.py. Mirrors the version pin in requirements.txt.
ansible-core>=2.18,<2.19
