# Dependências Python do venv efêmero/persistente (instaladas por run.sh).
ansible-core>=2.18,<2.19
# proxmoxer + requests: necessários apenas para o caminho SECUNDÁRIO
# (inventário dinâmico community.general.proxmox via API). Mantidos aqui
# para que o caminho SSH/futuro funcione sem alterar requirements.
proxmoxer>=2.0
requests>=2.31
