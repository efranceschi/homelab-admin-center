# Python dependencies for the ephemeral/persistent venv (installed by run.sh).
ansible-core>=2.21.0,<2.22
# proxmoxer + requests: required only for the SECONDARY path
# (community.general.proxmox dynamic inventory via API). Kept here so the
# SSH/future path works without changing requirements.
proxmoxer>=2.0
requests>=2.34.2
